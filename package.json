{
  "name": "ts-telegram-bot",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "add_date_to_config.sh && esbuild bot.ts --bundle --platform=node --outfile=dist/telegram-bot.js"
  },
  "dependencies": {
    "config": "^3.3.10",
    "grammy": "^1.20.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/config": "^3.3.3",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}
